/**
 * 
 */
package enumTypes;

/**
 * Enum that can possibly used somewhere in the framework. At the moment it's not 
 * being used anywhere.
 * 
 * @author mubari
 *
 */
public enum InventoryType
{
	DASHBOARD_GRAPH, DASHBOARD_METRICS, OPPORTUNITIES, MASTER_DATA, CONFIGURATION, CALCULATIONS
}
